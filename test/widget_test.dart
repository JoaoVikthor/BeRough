import 'package:flutter_test/flutter_test.dart';

void main() {
  // Este é um teste vazio que sempre passa.
  // Ele serve apenas para o Flutter não reclamar de erros de compilação
  // enquanto estamos focados em construir as telas do app.
  test('Teste dummy para evitar erros de compilação', () {
    expect(true, isTrue);
  });
}